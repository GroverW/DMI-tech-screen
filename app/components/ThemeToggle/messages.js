/*
 * ThemeToggle Messages
 *
 * This contains all the text for the ThemeToggle component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ThemeToggle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ThemeToggle component!',
  },
});
