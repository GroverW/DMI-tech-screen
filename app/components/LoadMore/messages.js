/*
 * LoadMore Messages
 *
 * This contains all the text for the LoadMore component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoadMore';

export default defineMessages({
  main: {
    id: `${scope}.main`,
    defaultMessage: 'Load More',
  },
});
